From 3d7fe2cb9624e266aa4d86f685d776618d0922fd Mon Sep 17 00:00:00 2001
From: Matt Campbell <matt@battleshopper.com>
Date: Mon, 30 Mar 2026 17:04:36 -0500
Subject: [PATCH 06/10] Refine heartbeat settings UI

---
 .../src/components/dialog-select-model.tsx    |   7 +-
 .../app/src/components/settings-heartbeat.tsx | 109 ++++++++++++++++--
 2 files changed, 102 insertions(+), 14 deletions(-)

diff --git a/packages/app/src/components/dialog-select-model.tsx b/packages/app/src/components/dialog-select-model.tsx
index cb688c30a..a8c9d198e 100644
--- a/packages/app/src/components/dialog-select-model.tsx
+++ b/packages/app/src/components/dialog-select-model.tsx
@@ -17,13 +17,14 @@ const isFree = (provider: string, cost: { input: number } | undefined) =>
   provider === "opencode" && (!cost || cost.input === 0)
 
 type ModelState = ReturnType<typeof useLocal>["model"]
+export type ModelSelectorState = Pick<ModelState, "list" | "visible" | "current" | "set">
 
 const ModelList: Component<{
   provider?: string
   class?: string
   onSelect: () => void
   action?: JSX.Element
-  model?: ModelState
+  model?: ModelSelectorState
 }> = (props) => {
   const model = props.model ?? useLocal().model
   const language = useLanguage()
@@ -89,7 +90,7 @@ type ModelSelectorTriggerProps = Omit<ComponentProps<typeof Kobalte.Trigger>, "a
 
 export function ModelSelectorPopover(props: {
   provider?: string
-  model?: ModelState
+  model?: ModelSelectorState
   children?: JSX.Element
   triggerAs?: ValidComponent
   triggerProps?: ModelSelectorTriggerProps
@@ -191,7 +192,7 @@ export function ModelSelectorPopover(props: {
   )
 }
 
-export const DialogSelectModel: Component<{ provider?: string; model?: ModelState }> = (props) => {
+export const DialogSelectModel: Component<{ provider?: string; model?: ModelSelectorState }> = (props) => {
   const dialog = useDialog()
   const language = useLanguage()
 
diff --git a/packages/app/src/components/settings-heartbeat.tsx b/packages/app/src/components/settings-heartbeat.tsx
index 628fda355..f7046b9a9 100644
--- a/packages/app/src/components/settings-heartbeat.tsx
+++ b/packages/app/src/components/settings-heartbeat.tsx
@@ -1,9 +1,13 @@
 import { Button } from "@opencode-ai/ui/button"
+import { Select } from "@opencode-ai/ui/select"
 import { Switch } from "@opencode-ai/ui/switch"
 import { showToast } from "@opencode-ai/ui/toast"
-import { onMount, Show, type Component } from "solid-js"
+import { useNavigate } from "@solidjs/router"
+import { createMemo, onMount, Show, type Component } from "solid-js"
 import { createStore } from "solid-js/store"
+import { useModels } from "@/context/models"
 import { mockingbirdJson } from "@/utils/mockingbird"
+import { ModelSelectorPopover, type ModelSelectorState } from "./dialog-select-model"
 import {
   MockingbirdCard,
   MockingbirdInput,
@@ -33,6 +37,7 @@ type HeartbeatRuntimeConfig = {
 
 type HeartbeatStatus = {
   config: HeartbeatRuntimeConfig
+  sessionTitle: string | null
   state: {
     sessionId: string | null
     running: boolean
@@ -54,6 +59,16 @@ type RuntimePayload = {
   path: string
 }
 
+type AgentType = {
+  id: string
+  mode?: string
+  hidden?: boolean
+}
+
+type AgentsPayload = {
+  agentTypes: AgentType[]
+}
+
 type HeartbeatFormState = {
   enabled: boolean
   interval: string
@@ -141,6 +156,8 @@ function buildHeartbeatPatch(form: HeartbeatFormState): HeartbeatRuntimeConfig {
 }
 
 export const SettingsHeartbeat: Component = () => {
+  const models = useModels()
+  const navigate = useNavigate()
   const [state, setState] = createStore({
     loading: true,
     saving: false,
@@ -148,6 +165,7 @@ export const SettingsHeartbeat: Component = () => {
     error: "",
     runtimeHash: "",
     runtimePath: "",
+    agentOptions: [] as string[],
     status: null as HeartbeatStatus | null,
     form: {
       enabled: true,
@@ -163,17 +181,59 @@ export const SettingsHeartbeat: Component = () => {
     } as HeartbeatFormState,
   })
 
+  const agentOptions = createMemo(() => {
+    const options = state.agentOptions
+    if (options.includes(state.form.agentId)) return options
+    return state.form.agentId ? [state.form.agentId, ...options] : options
+  })
+
+  const currentModel = createMemo(() => {
+    const value = state.form.model.trim()
+    if (!value) return
+    const slash = value.indexOf("/")
+    if (slash === -1) return
+    const providerID = value.slice(0, slash)
+    const modelID = value.slice(slash + 1)
+    if (!providerID || !modelID) return
+    return models.find({ providerID, modelID })
+  })
+
+  const heartbeatSessionHref = createMemo(() => {
+    const sessionId = state.status?.state.sessionId?.trim()
+    if (!sessionId) return null
+    return `/session/${sessionId}`
+  })
+
+  const heartbeatModelSelector: ModelSelectorState = {
+    list: models.list,
+    current: currentModel,
+    visible: models.visible,
+    set(item, options) {
+      if (!item) return
+      models.setVisibility(item, true)
+      if (options?.recent) models.recent.push(item)
+      setState("form", "model", `${item.providerID}/${item.modelID}`)
+    },
+  }
+
   async function load() {
     setState("loading", true)
     setState("error", "")
     try {
-      const [heartbeat, runtime] = await Promise.all([
+      const [heartbeat, runtime, agents] = await Promise.all([
         mockingbirdJson<HeartbeatStatusPayload>("/api/mockingbird/heartbeat"),
         mockingbirdJson<RuntimePayload>("/api/mockingbird/runtime/config"),
+        mockingbirdJson<AgentsPayload>("/api/mockingbird/agents"),
       ])
       setState("status", heartbeat.heartbeat)
       setState("runtimeHash", runtime.hash)
       setState("runtimePath", runtime.path)
+      setState(
+        "agentOptions",
+        agents.agentTypes
+          .filter((item) => item.mode !== "subagent" && !item.hidden)
+          .map((item) => item.id),
+      )
       setState("form", toForm(heartbeat.heartbeat.config))
     } catch (error) {
       setState("error", error instanceof Error ? error.message : "Failed to load heartbeat settings")
@@ -274,11 +334,25 @@ export const SettingsHeartbeat: Component = () => {
               <MockingbirdMetaRow label="Config file" value={state.runtimePath || "Unavailable"} />
               <MockingbirdMetaRow label="Enabled" value={status().config.enabled ? "Yes" : "No"} />
               <MockingbirdMetaRow label="Session id" value={status().state.sessionId ?? "Not created yet"} />
+              <MockingbirdMetaRow label="Session title" value={status().sessionTitle ?? "Heartbeat"} />
               <MockingbirdMetaRow label="Currently running" value={status().state.running ? "Yes" : "No"} />
               <MockingbirdMetaRow label="Last run" value={formatTimestamp(status().state.lastRunAt)} />
               <MockingbirdMetaRow label="Next due" value={formatTimestamp(status().nextDueAt)} />
               <MockingbirdMetaRow label="Last result" value={formatResult(status().state.lastResult)} />
               <MockingbirdMetaRow label="Last updated" value={formatTimestamp(status().state.updatedAt)} />
+              <Show when={heartbeatSessionHref()}>
+                {(href) => (
+                  <div class="flex justify-end">
+                    <Button
+                      variant="ghost"
+                      size="small"
+                      onClick={() => navigate(href())}
+                    >
+                      Open heartbeat session
+                    </Button>
+                  </div>
+                )}
+              </Show>
             </MockingbirdSettingsSection>
 
             <Show when={status().state.lastError}>
@@ -313,11 +387,18 @@ export const SettingsHeartbeat: Component = () => {
           <div class="grid gap-4 sm:grid-cols-2">
             <div class="flex flex-col gap-2">
               <span class="text-12-medium text-text-weak">Model</span>
-              <MockingbirdInput
-                value={state.form.model}
-                placeholder="openai/gpt-5.4"
-                onInput={(event) => setState("form", "model", event.currentTarget.value)}
-              />
+              <ModelSelectorPopover model={heartbeatModelSelector}>
+                <Button
+                  as="div"
+                  variant="ghost"
+                  size="normal"
+                  class="w-full min-w-0 justify-between rounded-lg border border-border-weak-base bg-surface-base px-3 py-2 text-13-regular text-text-strong hover:bg-surface-base"
+                >
+                  <span class="truncate text-left">
+                    {(currentModel()?.name ?? state.form.model) || "Select model"}
+                  </span>
+                </Button>
+              </ModelSelectorPopover>
             </div>
             <div class="flex flex-col gap-2">
               <span class="text-12-medium text-text-weak">Interval</span>
@@ -329,10 +410,16 @@ export const SettingsHeartbeat: Component = () => {
             </div>
             <div class="flex flex-col gap-2">
               <span class="text-12-medium text-text-weak">Agent id</span>
-              <MockingbirdInput
-                value={state.form.agentId}
-                placeholder="build"
-                onInput={(event) => setState("form", "agentId", event.currentTarget.value)}
+              <Select
+                options={agentOptions()}
+                current={state.form.agentId}
+                onSelect={(value) => value && setState("form", "agentId", value)}
+                placeholder="Select agent"
+                variant="ghost"
+                size="normal"
+                class="w-full capitalize text-text-base"
+                valueClass="truncate text-13-regular text-text-base"
+                triggerProps={{ "data-action": "heartbeat-agent" }}
               />
             </div>
             <div class="flex flex-col gap-2">
-- 
2.53.0

